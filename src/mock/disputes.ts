// Mock de disputas — solo para desarrollo local
export const mockDisputes: {
  id: string;
  amount: number;
  currency: string;
  status: string;
  reason: string;
  customerName: string;
  createdAt: string;
}[] = [];
